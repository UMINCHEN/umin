# UMIN Portfolio - Project Memory

這份文件記錄專案的設計準則、使用者偏好與既有決策，供未來任何 Claude session 沿用。
**完整設計系統（token / 元件）請看 [`design.md`](design.md)。**

---

## 專案基本資訊

- **用途**：產品設計師個人作品集（單頁式網站）
- **本機路徑**：`C:/Users/USER/Desktop/uminportfolio/`
- **GitHub**：https://github.com/UMINCHEN/uminportfolio
- **線上預覽**：https://uminchen.github.io/uminportfolio/ （GitHub Pages，main 分支根目錄）
- **檔案結構**：
  - `index.html` - 單一頁面，所有區段 + 全螢幕彈窗
  - `style.css` - 全部樣式（token 定義於 `:root`）
  - `design.md` - 設計系統文件（token、元件、規則）
  - `images/` - 圖片資料夾（user 自行放圖）
  - `design-system.html` - **已過時**（舊暖色版的 token 展示，未對外連結；新系統請以 `design.md` 為準）

---

## 設計方向（2026 改版：像素 / 深綠底 / 亮橘主色）

> 已整站重做為像素 / retro。以下為現行方向，細節見 `design.md`。

- **底色深綠、主色亮橘、頁尾深色**（色層少、降低填充）：底 `#0F1712`、主色 `--accent #FF8A3C`、頁尾用 `--surface`。亮橘上一律**深字 `#1A1005`**（白字對比不足）。
- **卡片透明**（露出底色綠 + 格線），只有必要面板才填色。**區塊本身無 hover 特效**。
- **像素 / retro**：結構容器直角、卡片硬陰影；**chips 用 4px 圓角、按鈕全圓角（pill）**；**按鈕用像素字體且無陰影**；chips **透明無底色、無框**、文字前加**白色 `#`**（CSS `::before`）。像素細節：**角釘 `--stud = --surface-2`**、44px 格線畫布、長「>」箭頭 / 簡單粗漢堡（無外框）/ 像素信封圖示。**站內不使用虛線**。
- **字體**：內文黑體 Noto Sans TC；**大標題、導航列、卡片標題、彈窗標題都用中文像素字 Zpix**（哩賀! / 我是 UMIN / 感謝您的時間! / 歡迎常來逛逛…，經 jsDelivr `@font-face` 載入）；**按鈕也用 Zpix**；**卡片年份也用 Zpix**（同卡片標題）、步驟編號用 Press Start 2P。**內文段落一律白字 `--ink`**（`--ink-mid` 只留給年份 / 導航 / 佔位說明等 meta）。
- **頁尾**：**黑底 `#07080B` + 淡像素星空**、加大高度，做出「滑到下一段」的斷差感；`id-line` 用主色橘、`聯絡我` 用像素信封、`LinkedIn` / `前往 VIP 特區` 用預設按鈕。
- **彈窗**：**內容區塊水平置中、文字靠左**；`modal__kicker` 顯示**完整專案名（不含年份）** 且用 Zpix；`case__h` 左橘邊。`了解更多`＝橘色實心 primary、`前往站台`＝預設框線。`哩賀!` 為白字。
- **像素圖示**：內嵌 SVG sprite（`shape-rendering: crispEdges`），隨字色。
- **減少動畫**：只有 `:hover`/`:active` 與彈窗淡入，並支援 `prefers-reduced-motion`。
- **避免 AI 套版**（承 taste-skill）：不用漸層、外發光、玻璃擬態、AI 紫、裝飾性圓點、scroll 提示、版本標籤。
- **不用 em-dash `—`**：用一般 `-` 或全形 `｜`。

### 內容處理
- **⚠️ 絕對不要修改任何使用者寫的文字**（口語、標點、特殊符號都保留，例：「哩賀!」「歡迎常來坐!」）。

---

## 既有功能與行為

- **導覽列**：固定頂部、永遠可見（不做隨捲動隱藏）。桌面單行：logo(UMIN) + 關於我 / 專案 / 聯絡 + 下載履歷按鈕；`≤720px` 為漢堡選單，點連結自動關閉。
- **Hero（關於我）**：左右分割（文字 + 形象照佔位），含 hashtag chips、單一 CTA「經歷與優勢」（開彈窗）。**下載履歷按鈕只在導覽列**，hero 本身不重複放。
- **專案**：長方卡片、圖片與文字**左右交錯**，每張含標題、標籤(chips，緊接標題下方)、描述、按鈕。**年份**：有案例彈窗的專案，年份顯示在彈窗白色標題內；沒有彈窗（純外部連結）的專案，年份留在卡片上。
- **全螢幕彈窗（滿版）**：「了解更多 / 經歷與優勢」用 `.modal` 開啟，**不跳頁**；可點背景 / 關閉鈕 / `Esc` 關閉。
- **圖片佔位 `.ph`**：綠面板 + 點陣 + 像素檔名（無虛線），等 user 換成 `<img>`。案例彈窗內每個內容區塊都預留一個 `.ph--wide`。

---

## 圖片管理

- **命名格式**：`{區段前綴}-{主題}.{副檔名}`
- **前綴**：`hero-`（形象照）/ `work-{專案}-cover`（卡片封面）/ `work-{專案}-{主題}`（彈窗內案例圖）
- 替換圖片：把 `<figure class="ph …" data-name="檔名">…</figure>` 換成 `<img src="images/檔名" alt="…" />`

---

## 雙語 / i18n 狀態

- **目前只有繁體中文**，未來考慮 `?lang=en` + JS 字典方案，暫不實作。

---

## Git 流程

- **預設行為：每次完成修改後直接 commit + push 到 `main`，不需要再問使用者**
- Commit message 用簡明英文，描述「為什麼」改而不是「改了什麼」
- GitHub Pages 會自動部署，約 1-2 分鐘生效
- 帳號為 `UMINCHEN`
- 例外：如果修改範圍很大、或涉及破壞性操作（刪除大量內容、改動架構），先確認再推

---

## 對話風格偏好（與使用者互動）

- 使用者是產品 / UI/UX 設計師，**不是工程師**
- 用視覺與設計語言溝通，少用艱深技術術語
- 重視美感與細節
- 修完問題後簡短報告：改了什麼、為什麼、線上連結
- 重點明確即可，不必長篇大論
